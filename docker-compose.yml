services:
  remote-app:
    container_name: htpc-remote-demo
    build:
      context: .
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_DEMO_MODE: ${NEXT_PUBLIC_DEMO_MODE:-false}
        NEXT_PUBLIC_HTPC_PLATFORM: ${NEXT_PUBLIC_HTPC_PLATFORM:-WINDOWS}
    environment:
      NEXT_PUBLIC_DEMO_MODE: ${NEXT_PUBLIC_DEMO_MODE:-false}
      NEXT_PUBLIC_HTPC_PLATFORM: ${NEXT_PUBLIC_HTPC_PLATFORM:-WINDOWS}
    restart: unless-stopped
    expose:
      - "3000"
    ports:
      - "${HTPC_REMOTE_BIND_IP:-0.0.0.0}:3000:3000"
    networks:
      - htpc-remote

networks:
  htpc-remote:
    name: htpc-remote
    driver: bridge
